var Backbone = require('backbone');
var BaseModel = require('../../../models/base_model.js');
module.exports = (function() {
	var SelectTemplateModel = BaseModel.extend({
  	});

  	return SelectTemplateModel;

})();